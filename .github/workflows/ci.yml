name: Continuous Integration
on:
  workflow_call:
    inputs:
      config-path:
        required: false
        type: string
    secrets:
      token:
        required: false
jobs:
  build-and-test:
    run: echo "Define your CI job steps here"
